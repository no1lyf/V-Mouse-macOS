# V-Mouse鼠标映射 架构与稳定性约束（v0.7.4）

## 1. 需求边界

- 每台设备初始提供侧键 1–12、改键后的 DPI+、DPI−、滚轮左键和滚轮右键；还可在默认或自定义分组中继续录入新输入，中键信号也允许录入。
- 映射目标支持组合键、Mos 分组的 mac快捷键、打开应用/文件/文件夹/网址、命令、文本、宏、配置切换，以及合并了按住型滚轮控制的鼠标动作；鼠标点击目标可包含中键。
- 侧键录入、侧键映射和拦截开关不写入鼠标，不修改板载按键、宏或灯光配置。
- 不显示当前 DPI，不读取或写入板载 DPI 档位；Windows 板载方式与 macOS 本机映射保持独立。
- 手动读取只识别当前板载配置下设备发出的信号，不猜测未知的厂商私有协议。
- Naga V2 HS 使用专用报告解码；其他手动启用的标准 HID 设备按 Usage Page/Usage 解析。设备族默认以 VID/PID 识别，可选增强规则要求所有已启用条件同时匹配。
- 侧键模拟中键与物理中键在当前标准 HID 报告和 CGEvent 中完全相同。按用户选择允许录入后，两者共享同一逻辑侧键、映射动作和拦截状态；物理左键、右键仍不允许录入。

## 2. 核心原则

优先级从高到低为：

1. 尽量不漏掉一次鼠标映射。
2. 对已开启“拦截板载键值”的来源，确认属于鼠标的原始按下、释放、自动重复和拖拽不能放行；关闭时板载键值直通且不执行 Mac 自定义键值。
3. 不能仅凭键码拦截，避免把物理键盘误认为鼠标。
4. 设备断开、Event Tap 失效、配置切换、宏取消和退出时不能留下合成键或修饰键。
5. 普通鼠标移动和左右键不参与映射；未录入中键信号时，物理中键保持普通行为。

Event Tap 事件本身不提供来源设备 ID，因此“非独占 + Event Tap”无法在理论上同时证明零漏拦截和零物理键盘误拦截。当前实现采用精确设备原始报告、按阶段的一次性令牌和每键校准的 3–10 ms 时间范围，把误判窗口压到最小；无法匹配令牌的系统事件必须放行，以保护物理键盘。这是本方案的系统边界，不以扩大时间窗口换取表面命中率。

## 3. 模块与数据流

### `HIDListener`

- 使用 `IOHIDManager` 以非独占、只读方式观察按键设备。
- 设备匹配后按设备族应用 VID/PID 基础身份，并在启用增强识别时同时校验所有已选条件；Naga 专用解码器还校验完整描述符和最大报告长度。
- 按固定报告布局解析键盘、修饰键、Consumer、System、额外鼠标键和横向滚轮相对量；不再解释 Report 5 的默认私有 DPI 内容。
- 按键路径不调用 `IOHIDDeviceSetReport`；整个应用不创建 `IOHIDUserDevice`，不使用 `kIOHIDOptionsTypeSeizeDevice`。
- 每个原始状态变化生成一个一次性关联令牌；原始释放同时直接通知 `ButtonMapper` 结束映射动作。
- 设备断开时清空令牌并释放全部合成输入。

### `HardwareCalibrationManager`

- 录入任意默认或自定义来源时都同时接收 HID 输入和 Event Tap 系统事件；每轮三次一致性校验共用 20 秒倒计时，成功、取消、失败或页面关闭都会停止计时器。
- DPI+、DPI− 只有在板载改成普通键值后才参与录入；默认私有 DPI 报告没有可拦截系统事件，超时后明确提示用户，不猜测、不固定绑定。
- 滚轮左右倾斜除普通键值和鼠标键外，还支持 Report 1 Consumer AC Pan 相对量；HID 方向与系统横向滚动方向分别保存，避免自然滚动设置导致左右颠倒。
- 每个来源要求三次一致样本，计算 HID→CGEvent 时间差的中位数和抖动容差。
- 只允许单键、修饰键+单键、中键、额外鼠标键和系统功能键。
- 已识别的功能键和常用组合键统一附加中文释义；旧录入数据在读取配置时自动刷新显示字符串。
- 拒绝物理左键、右键，拒绝重复的原始 HID 来源，拒绝三次不一致的宏或文本序列。
- 录入结果只保存在 Mac 本地。

### `EventCorrelationBroker`

- 令牌包含：设备身份、事件种类、键码/鼠标键/横向滚动方向、按下或释放、逻辑输入、预期时间和容差。
- 键盘、按键和修饰键的一个 HID 状态变化最多匹配一个系统事件。
- 默认令牌 100 ms 后销毁；真正可匹配的时间误差限制在 0.5–20 ms，手动校准限制在 3–10 ms。
- 按下令牌不能匹配释放，键盘令牌不能匹配鼠标事件；滚轮左右倾斜的横向脉冲必须同时匹配方向和时间。

### `EventTapManager`

- 录入时使用 listen-only Event Tap，不阻断任何事件。
- 运行时使用可拦截 Event Tap，监听 keyDown、keyUp、flagsChanged、鼠标按下/释放/拖拽、横向滚轮和系统功能事件。
- 回调中不读 JSON、不等待 HID、不休眠、不执行宏；只做内存匹配和状态更新。
- 合成事件带私有标记并直接放行，防止递归映射。
- 每个录入来源独立读取 `interceptEnabled`；关闭时不创建映射令牌，原事件直接通过。
- 修饰键按左右键码分别跟踪，不依赖聚合 flags 猜测释放。
- 原始 HID release 到达 30 ms 后会兜底删除残留活动状态；对应的一次性 release 令牌仍可拦截稍后到达的系统 keyUp。
- Event Tap 被系统停用时先释放所有合成输入和修饰键状态，再立即重新启用同一 Tap。

### `ScrollReversalManager` / `ScrollSmoothingEngine`

- 使用独立的 `.cgAnnotatedSessionEventTap + .tailAppendEventTap + .defaultTap`，不依赖 Naga HID 报告或时间相关。
- 通过 `scrollPhase` 和 `momentumPhase` 排除触控板与惯性手势；`isContinuous` 和 `scrollCount` 不作为硬排除条件，因为 Naga 物理滚轮也可能携带这些值。
- 分别用整数和 Double API 反转 Delta、PointDelta、FixedPtDelta；垂直和水平反向独立。
- 平滑滚动支持垂直/水平轴、步长、速度、持续时间、死区和可选的触控板阶段模拟；所有状态在主队列串行更新，合成滚轮事件带私有标记，避免回流处理。
- 加速、纵向转横向和临时禁用平滑均为按住型状态。键盘快捷键始终放行原键；侧键动作在 HID release、停用映射、设备断开和退出时统一释放。
- 关闭滚轮增强后立即停止尚未完成的平滑输出，不保留惯性或按住状态。
- 被系统停用后自动重新启用；界面显示真实的运行、停止或失败状态。

### `ButtonMapper`

- 所有动作在单一串行队列执行。
- 长按组合键使用按键引用计数，多个动作共用修饰键时不会提前释放。
- 按修饰键→主键顺序按下，按相反顺序释放。
- 宏延时使用可取消的异步调度；关闭映射后未执行步骤全部失效。
- 统一的幂等清理只释放本程序记录的合成键，不修改物理键盘状态。
- 文本动作临时使用剪贴板，且仅在剪贴板仍属于本次动作时恢复原内容。
- 动作编辑器固定为“按键、鼠标、mac快捷键、打开、命令、文本、配置”；旧 `systemAction` 和 `scrollControl` 配置无需改写即可显示在新分类中。
- mac快捷键按 Mos 的功能键、应用与窗口、文档编辑、访达操作、系统功能、截图、导航和修饰键分组，并解析为受控的 `KeyStroke`；纯修饰键按住生效并进入统一释放状态机。
- UI 使用原生 AppKit 语义颜色、系统强调色、标准分组框和 SF Symbols，并提供 Apple 毛玻璃、黑夜、原始黑色三种可持久化主题；`L10n` 在应用内持久化简体中文、英文、日文、韩文和德文选择，五种语言完整覆盖可见状态、错误、键值定义与教程。
- 映射页使用统一 4×4 等宽卡片，编辑动作窗口加宽；主界面提供独立教程入口，各主要页面底部显示当前页面的安全与使用说明。
- 滚轮控制并入鼠标动作，只记录按住来源；释放和全局清理均幂等，不会留下永久加速、横向或禁用平滑状态。打开目标通过 `NSWorkspace` 执行，不拼接 shell 命令。

### `InputCoordinator`

- 集中管理 `idle`、`preparing`、`waitingForDevice`、`active`、`calibrating`、`unavailable` 状态。
- 启用前检查精确设备、输入监控和辅助功能权限。
- 设备断开时立即停止 Event Tap 并进入等待；同一会话重新连接后自动恢复用户请求的映射。
- 校准期间关闭运行时拦截，避免录入事件触发现有映射。

### `ConfigManager` / 输入分组

- 配置文件保存 Mac 动作，设备保存板载键值、输入名称、分组、身份规则和当前挂载状态；同一配置可以挂载到多台设备。
- 设备最多启用一个配置，也可以显式处于零配置状态。移除最后一个配置只解除设备挂载，不删除配置本身。
- 默认分组与自定义分组使用同一排序模型，均可改名、移动，并在清空后删除；新输入按“自定义键 1、2……”生成不冲突的顺序名称。
- JSON 更新经过集中串行化、校验和原子替换；迁移先复制旧数据，再规范化设备、配置和分组关系，避免部分写入。

### `BuiltInTrackpadManager`

- 主页面选项只表达应用策略：“应用运行且外接鼠标存在时，请求系统忽略内置触控板”。
- 进入策略前记录系统有效状态；关闭选项、外接鼠标断开或应用退出时恢复进入前状态。
- 系统策略写入通过运行时解析的 macOS 非公开接口完成，并从 HID Event Service 与触控板偏好交叉读取结果；接口不可用或结果不可验证时按失败处理，不把 UI 开关当作已生效。

## 4. 原始事件处理

- Report 1：读取中键、额外鼠标键和 Consumer AC Pan 横向滚轮；左右键、移动保持系统原行为。中键只有在被明确录入并开启拦截时才映射。
- Report 2：读取 Consumer Control 状态变化。
- Report 3：读取 System Control 状态变化。
- Report 4：不参与映射。
- Report 5：默认私有 DPI 内容不参与录入或运行时映射，保持只读忽略。
- Report 6：读取左右修饰键和六键无冲键盘 usage 的集合差异。

报告只接受固定长度或“固定长度 + 明确报告 ID 字节”两种形式；未知编号和长度直接忽略。未绑定但确认来自 Naga 的键盘/修饰键序列仍会产生无动作令牌，使板载宏尾部尽量不泄漏到应用。

滚轮增强完全独立于 HIDListener，只需要辅助功能权限。只开启滚轮增强时不启动侧键 HID 监听、不创建侧键令牌，也不要求连接 Naga；因此不会暗中恢复任何侧键拦截。

## 5. 权限与签名

运行需要用户授予：

- 输入监控：只读接收 Naga 原始 HID 按键报告；
- 辅助功能：拦截系统事件并发送映射动作；
- 蓝牙：仅在读取蓝牙电量时使用。

不需要 Apple 特殊虚拟 HID entitlement，也不需要为了开发构建申请 Apple 授权。ad-hoc 签名适合当前测试；正式发布再进行 Developer ID 签名和公证。重新构建或更换安装路径后，macOS 可能要求重新确认隐私权限。

首次缺少权限时只显示一个中/英/日/韩/德固定顺序的应用内说明，不自动连续调用系统辅助功能与输入监控申请接口，避免重复弹窗或空白选择框。权限条目仍由用户在系统设置中手动管理。

## 6. 验证要求

自动检查：

- 精确描述符接受、错误 Vendor 拒绝；
- Report 1–6 固定长度和未知编号拒绝；
- 常用 HID usage 到 macOS keyCode 的转换；
- 令牌按种类、阶段、时间匹配且只能消费一次；
- 鼠标/触控板滚动特征分类，以及整数、点数、定点 delta 的反转；
- 滚轮参数边界、应用默认值、按住型控制释放和设置持久化；
- 自定义输入分组迁移、排序、删除和顺序命名；
- VID/PID 设备族、全部增强条件匹配和同型号设备隔离；
- 内置触控板策略的有效值读取与不可验证状态拒绝；
- 长延时宏可以在 500 ms 内取消并完成释放。

真实鼠标回归：

- 侧键 1–12、改键后的 DPI± 与滚轮左右键单击、长按、快速连按和交叉同时按；
- 板载单键及修饰键+单键；
- 同时敲击物理键盘相同键码，确认不误触发映射；
- 映射为 Command/Shift 等长按动作后拔鼠标、关闭映射、退出程序；
- 左右键、移动、拖拽和触控板不受无关影响；未录入中键时验证物理中键原行为，录入后验证其中键联动符合提示；
- 睡眠/唤醒、接收器拔插、Event Tap 权限撤销与恢复；
- 8 小时持续运行和高频输入。
