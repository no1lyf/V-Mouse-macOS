# V-Mouse鼠标映射：构建与安装

V-Mouse鼠标映射是一款 macOS 鼠标按键二次映射工具。应用只读取鼠标输出并在 Mac 软件层执行映射，不会写入鼠标固件或板载配置。

## 生成安装包

在项目目录运行：

```bash
bash Scripts/build_app.sh
```

脚本将：

1. 编译 Release 版本；
2. 运行内置 HID 与配置自检；
3. 生成带正式产品图标、ad-hoc 签名的 `V-Mouse鼠标映射-v版本.app`；
4. 生成同版本号、无日期后缀的 DMG 安装包并验证映像。

## 安装

打开生成的 DMG，将 `V-Mouse鼠标映射` 拖到“应用程序”文件夹。首次使用按键二次映射时，请在“系统设置 → 隐私与安全性”中为当前版本开启：

- 辅助功能；
- 输入监控。

蓝牙权限仅用于读取支持设备的电量。

## 兼容性说明

产品显示名称已改为“V-Mouse鼠标映射”，但内部包标识、可执行文件名和配置存储路径保持不变。这能让升级版本继续读取现有配置，并降低权限记录失效的风险。

单独运行安装包内的自检：

```bash
'V-Mouse鼠标映射-v0.7.4.app/Contents/MacOS/NagaController' --self-test-hid-codec
```
