# V-Mouse鼠标映射 v0.7.4 开源说明

## 项目用途

V-Mouse鼠标映射是一款运行于 macOS 13 及更高版本的鼠标按键二次映射工具。它读取鼠标已经发送给 Mac 的板载键值，在软件层决定让原输入直通、屏蔽原输入，或执行用户设置的 Mac 动作。软件不修改鼠标固件、板载按键、DPI、灯光或宏。

## 当前版本功能

- 读取 Razer Naga V2 HyperSpeed 及用户手动选择的标准 HID 鼠标/键盘输入。
- 默认提供侧键、DPI 和滚轮倾斜输入；支持新建自定义分组、读取新输入、自动编号、改名、移动、排序和清空后删除。
- 将板载输入二次映射为按键序列、Mac 快捷键、鼠标与系统动作、打开应用/文件/文件夹/网址、命令、文本、宏、配置切换和按住型滚轮控制。
- 每个输入可独立选择直通或拦截；包含 ⌘ 或 Control 的板载值可单独启用两者互换。
- 配置可在多台设备间共用；设备可以不启用配置，最后一个配置也可从设备移除而不删除配置文件。
- 设备身份默认以 VID/PID 为基础；每个设备族可选择启用增强条件，且所有启用条件必须同时匹配。
- 提供平滑、方向、轴向、步长、速度、持续时间、死区、模拟触控板阶段和按住型控制等滚轮设置。
- 可在外接鼠标连接且应用运行时请求 macOS 停用内置触控板，并在关闭选项、断开鼠标或退出应用时恢复。
- 提供简体中文、英语、日语、韩语和德语界面与帮助。

## 主要实现方式

- 使用 Swift、AppKit、IOKit/IOHIDManager 和 Core Graphics Event Tap 构建原生菜单栏应用。
- HID 监听以只读、非独占方式打开设备；Razer Naga 使用专用报告解码，标准 HID 设备按 Usage Page/Usage 解析。
- 每个 HID 按下/释放事件生成短时、一次性的来源令牌；系统事件只有在类型、阶段、键码和校准时间戳一致时才会被拦截，以隔离物理键盘和多台同型号设备。
- 配置以本机 JSON 保存，更新通过集中配置管理和原子替换完成；旧数据启动时迁移到设备优先模型。
- 滚轮处理排除带原生滚动阶段或惯性阶段的触控板事件；平滑输出由串行状态机管理，并在停用、设备断开或退出时统一释放。
- 内置触控板功能通过系统的“外接鼠标存在时忽略内置触控板”策略接口进行请求，并由应用保存、恢复进入前状态。该接口属于 macOS 非公开行为，系统版本变化可能影响可用性。
- 构建包含 XCTest、HID 编解码、迁移、事件来源、多设备隔离和五语言完整性自检。

## 权限与数据

按键二次映射需要“辅助功能”和“输入监控”权限；蓝牙权限仅用于读取支持设备的电量。配置和输入定义只保存在本机，项目不包含网络上传逻辑。命令动作会执行用户填写的本地命令，应只导入和运行可信配置。

## 第三方代码与许可

本项目的部分代码来自或修改自：

- [DParent10/NagaController](https://github.com/DParent10/NagaController)，MIT License。
- [Caldis/Mos](https://github.com/Caldis/Mos)，CC BY-NC 4.0。

组合发布版本受 Mos 的非商业条款约束，仅可用于非商业用途。再发布时必须保留 `LICENSE` 和 `THIRD_PARTY_NOTICES.md`，注明来源及修改。商业使用前须取得相关权利人的许可，或替换所有受非商业条款约束的实现。

## 构建与验证

```bash
bash Scripts/build_app.sh
```

脚本生成 `V-Mouse鼠标映射-v0.7.4.app` 和 `V-Mouse鼠标映射-v0.7.4.dmg`，文件名只包含版本号，不带日期后缀。安装包使用 ad-hoc 签名，未使用 Apple Developer ID 公证；首次打开时可能需要在“系统设置 → 隐私与安全性”中确认。
